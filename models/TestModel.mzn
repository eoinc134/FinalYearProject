int: numStudents;
int: maxGroupSize;
int: timeSlots; %of len = 2hrs
int: maxS;

output ["numStudents: \(numStudents)\n",
        "maxGroupSize: \(maxGroupSize)\n",
        "timeSlots: \(timeSlots)\n",
        "maxS: \(maxS)"];